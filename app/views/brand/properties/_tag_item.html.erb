<span class="inline-flex items-center gap-1 px-3 py-1 rounded-full text-sm bg-blue-100 text-blue-800">
  <%= property.text_value %>
  <%= button_to brand_property_path(brand, property),
      method: :delete,
      form: { data: { turbo: true, turbo_action: "replace" } },
      class: "hover:bg-blue-200 rounded-full p-0.5" do %>
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
    </svg>
  <% end %>
</span>
