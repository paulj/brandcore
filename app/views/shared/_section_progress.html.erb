<%
  # Section Progress Partial
  # Displays a progress indicator for a brand component section
  #
  # Required local variables:
  # - presenter: An object that includes SectionProgress module
  #   Must respond to: #completion_count, #completion_percentage
  #
  # Example usage:
  # <%= render "shared/section_progress", presenter: @vision_presenter % >
%>
<div class="bg-gradient-to-r from-gray-50 to-white border border-gray-200 rounded-xl p-6 mb-8" id="section_progress">
  <div class="flex items-center justify-between mb-4">
    <h3 class="text-lg font-semibold text-gray-900">Section Progress</h3>
    <span class="text-sm text-gray-500"><%= presenter.completion_count %></span>
  </div>
  <div class="w-full bg-gray-200 rounded-full h-2">
    <div class="bg-black h-2 rounded-full transition-all duration-300" style="width: <%= presenter.completion_percentage %>%"></div>
  </div>
</div>
